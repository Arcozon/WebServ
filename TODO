Config:
	`server`				| 1–N
Server:
	`host`					| 1
	`port`					| 1
	`server_name`			| 0–N
	`client_max_body_size`	| 0–1
	`error_page`			| 0–N
	`location`				| 0–N
	`return`				| 0–1
Location:
	`root`					| 0–1
	`index`					| 0–N
	`allow`					| 0–1
	`autoindex`				| 0–1
	`upload_store`			| 0–1
	`return`				| 0–1
	`cgi_handler`			| 0–N
	`error_page`			| 0–N


Check if server Direcctive are valid (HTTP error, name already set, location existing ...)
Do multiple servers

-	CGI
	- Unset var before adding them
	- Set vars
		REQUEST_METHOD
		QUERY_STRING
		CONTENT_TYPE
		CONTENT_LENGTH
		SCRIPT_NAME
		PATH_INFO
		PATH_TRANSLATED
		SERVER_NAME
		SERVER_PORT
		SERVER_PROTOCOL
		REMOTE_ADDR
		REMOTE_PORT
		GATEWAY_INTERFACE
	- Cd
	- execve
